# Generated App Workspace

This folder is the dedicated SDLC assistant output workspace.

- The CLI reads and writes application code only inside `repo/app/`
- Your main project source outside `repo/app/` should remain untouched
